alias cp = cp -iv
alias mv = mv -iv
alias rm = rm -vI
alias bc = bc -ql
alias rsync = rsync -vrPlu
alias mkd = mkdir -v
alias ffmpeg = ffmpeg -hide_banner

alias ls = eza
alias grep = rg
alias cat = bat --style=plain --color=always
alias du = dust

alias neofetch = fastfetch

alias ka = killall
alias g = git
alias sdn = shutdown -h now
alias v = neovide --maximized --no-vsync --no-tabs --fork

